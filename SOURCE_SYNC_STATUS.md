# Source sync status

Current target: **Cardcha: Shardbound v0.1.17-alpha.11.8**.

The repository handoff/history is current through alpha.11.8. The full working source + runtime PNG assets are packaged in the matching Windows Builder ZIP produced in the development session.

A connector-side bulk upload attempt was stopped before completion, so no incomplete bootstrap chunks were left in the repository. Until the one-time full source import is completed, do not treat this repository as the only build source.

Alpha.11.8 changes:
- temporary MiMi mystery Town test window 10:00–15:00;
- floating world `???` label removed;
- six MiMi portrait expressions with context-specific `$0`–`$5` dialogue portrait commands;
- MiMi normal/story/broom presentation increased by about 15% (2.0x -> 2.3x where custom-rendered);
- ChaCha renders 2x beside MiMi with wider spacing and safer follower offsets.
