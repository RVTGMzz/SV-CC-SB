namespace Cardcha.Patches;

/// <summary>
/// alpha26.5.3: retired. The one-button reveal behavior now lives directly in CardchaRevealMenu.
/// Kept as an empty type for source/history continuity; no Harmony runtime patch is installed.
/// </summary>
internal static class RevealControllerOneButtonRuntimePatch
{
}
