name: Cardcha alpha27.0.7.7.3 social + inventory UI polish

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "0.7.7.3 workflow archived; use 0.7.7.4 workflow for current alpha.27 source."
