name: Cardcha alpha28.0.4.14.3.7 Auto Scenario Runner

on:
  push:
    branches: [ cardcha-alpha28-056-card-test-lab ]

permissions:
  contents: write

jobs:
  build:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply current Card Lab generators
        shell: bash
        run: |
          set -euo pipefail
          # Older Arena/UI finalizers are already materialized in branch source.
          # Replaying them would try to patch pre-.3.6 labels and regress the current Lab.
          python3 tools/alpha28_41436_auto_audit.py
          python3 tools/alpha28_41436_icon_lock.py
          if ! grep -q 'CardAutoScenarioRunnerService CardAutoRunner' src/Cardcha/ModEntry.cs; then
            python3 tools/alpha28_41437_auto_scenario.py
          else
            echo 'Auto Scenario Runner already materialized; skipping one-time source patch.'
          fi
          python3 tools/alpha28_41432_version.py

      - name: Source acceptance checks
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import json, hashlib, struct, xml.etree.ElementTree as ET
          root = Path('src/Cardcha')
          version = '0.3.0-alpha.28.0.4.14.3.7'

          assert json.loads((root/'manifest.json').read_text())['Version'] == version
          assert f'<Version>{version}</Version>' in (root/'Cardcha.csproj').read_text()
          assert version in (root/'Directory.Build.targets').read_text()

          entry = (root/'ModEntry.cs').read_text()
          menu = (root/'UI/CardTestLabMenu.cs').read_text()
          runner = (root/'Services/CardAutoScenarioRunnerService.cs').read_text()
          save = (root/'Services/SaveService.cs').read_text()
          combat = (root/'Services/CombatService.cs').read_text()
          core = (root/'Services/CoreCardEffectsService.cs').read_text()
          drops = (root/'Services/DropService.cs').read_text()
          gacha = (root/'Services/GachaService.cs').read_text()
          arena = (root/'Services/CardTestArenaService.cs').read_text()
          audit = json.loads((root/'assets/card_auto_audit.json').read_text())

          assert 'CardAutoScenarioRunnerService CardAutoRunner' in entry
          assert 'cardcha_card_auto_run' in entry
          assert 'new CardTestLabMenu(this.CardLab, this.Renderer, this.CardArena, this.CardAutoRunner)' in entry
          assert f'Cardcha! v{version} CARD AUTO SCENARIO TEST' in entry
          assert 'AUTO RUN ALL [L3/F5]' in menu
          assert 'AUTO SCENARIO:' in menu
          assert 'CardAutoScenarioStatus.Blocked' in runner
          assert '"victory_charge"' in runner and 'Boss Energy subsystem does not exist yet' in runner
          assert 'for (int level = 1; level <= max; level++)' in runner
          assert runner.count('=> this.Check') >= 70

          assert 'BeginTransientTestScope' in save and 'TransientTestDepth > 0' in save
          assert 'BeginScenarioTestScope' in drops and 'DebugSetForcedRolls' in drops
          assert 'BeginScenarioTestScope' in gacha and 'DebugSetForcedRolls' in gacha
          assert 'CardPassiveDebugSnapshot' in core and 'DebugForceNextChanceRoll' in core
          assert 'DebugSyncCompletion' in combat and 'DebugVitalityAppliedBonus' in combat

          assert len(audit) == 76
          assert sum(row['status'] == 'BLOCKED' for row in audit) == 1
          assert next(row for row in audit if row['id'] == 'victory_charge')['status'] == 'BLOCKED'
          assert "'CardAutoScenarioRunnerService.cs'" in Path('tools/alpha28_41436_auto_audit.py').read_text()

          assert 'FixedOutgoingRawDamage = 100' in arena
          assert 'KillTargetCount = 4' in arena
          assert 'KillTargetMaxHealth = 100' in arena
          assert 'PreferredWidth = 1776' in menu
          assert 'SelectionDebounceMs = 160L' in menu

          map_xml = ET.parse(root/'assets/card_test_arena.tmx').getroot()
          w, h = int(map_xml.attrib['width']), int(map_xml.attrib['height'])
          assert (w, h) == (18, 12)
          for layer in map_xml.findall('layer'):
              data = layer.find('data')
              tokens = [(x or '').strip() for x in (data.text or '').split(',')]
              assert data is not None and data.attrib.get('encoding') == 'csv'
              assert all(tokens) and len([int(x) for x in tokens]) == w*h

          icons = (root/'assets/card_icons.png').read_bytes()
          assert struct.unpack('>II', icons[16:24]) == (320, 1024)
          digest = hashlib.sha256(icons).hexdigest()
          assert digest == (root/'assets/card_icons.sha256.lock').read_text().strip().lower()
          assert digest == 'c5ff456e9a0a697a10537a391ec5abf2f90966de89299799fd2153356b1e6e41'
          assert hashlib.sha256((root/'assets/airship_visual.png').read_bytes()).hexdigest() == '1821ee869759a924f7ff2b6821aaeb64b80a000d84c46f207a578d3a1e771132'
          print('alpha28.0.4.14.3.7 source acceptance PASS')
          PY
          test "$(git hash-object src/Cardcha/assets/mimi_broom.png)" = "0410660a8ec2f3190b20882898c1897e67819f9d"
          test "$(git hash-object src/Cardcha/assets/mimi_profile.png)" = "2cecabf9d4249c0ee5801f32c7d780a0151a17c9"

      - uses: Pathoschild/SMAPI-ModBuildWorkflow/add-build-environment@v1

      - name: Compile test build
        run: dotnet build src/Cardcha/Cardcha.csproj -c Release -p:EnableModDeploy=false -p:EnableModZip=false

      - name: Materialize Auto Scenario source back to branch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/Cardcha tools/alpha28_41432_version.py tools/alpha28_41436_auto_audit.py tools/alpha28_41436_icon_lock.py tools/alpha28_41437_auto_scenario.py .github/workflows/cardcha-alpha28-056-card-test-lab.yml
          if ! git diff --cached --quiet; then
            git commit -m "chore: materialize alpha28.0.4.14.3.7 Auto Scenario Runner [skip ci]"
            git push origin HEAD:cardcha-alpha28-056-card-test-lab
          fi

      - name: Final regression gate
        shell: bash
        run: |
          set -euo pipefail
          test "$(sha256sum src/Cardcha/assets/card_icons.png | cut -d' ' -f1)" = "c5ff456e9a0a697a10537a391ec5abf2f90966de89299799fd2153356b1e6e41"
          test "$(sha256sum src/Cardcha/assets/airship_visual.png | cut -d' ' -f1)" = "1821ee869759a924f7ff2b6821aaeb64b80a000d84c46f207a578d3a1e771132"
          grep -q 'CollisionEdits=NONE' src/Cardcha/Services/AirshipFoundationService.cs
          grep -q 'LifeStealFractionalCarry' src/Cardcha/Services/CoreCardEffectsService.cs
          grep -q 'KillTargetCount = 4' src/Cardcha/Services/CardTestArenaService.cs
          grep -q 'BeginTransientTestScope' src/Cardcha/Services/SaveService.cs
          grep -q 'AUTO RUN ALL' src/Cardcha/UI/CardTestLabMenu.cs
          grep -q 'cardcha_card_auto_run' src/Cardcha/ModEntry.cs
          echo 'alpha28.0.4.14.3.7 regression gate PASS'

      - name: Package compiled test build
        shell: bash
        run: |
          set -euo pipefail
          rm -rf artifact stage
          mkdir -p artifact stage/Cardcha
          cp "$(find src/Cardcha/bin -name Cardcha.dll -print -quit)" stage/Cardcha/
          cp src/Cardcha/manifest.json src/Cardcha/config.json stage/Cardcha/
          cp -a src/Cardcha/assets src/Cardcha/i18n stage/Cardcha/
          test -s stage/Cardcha/Cardcha.dll
          test "$(python3 -c "import json; print(json.load(open('stage/Cardcha/manifest.json'))['Version'])")" = "0.3.0-alpha.28.0.4.14.3.7"
          (cd stage && zip -qr ../artifact/Cardcha_v0.3.0-alpha.28.0.4.14.3.7_CardAutoScenarioRunner_TEST.zip Cardcha)
          sha256sum artifact/*.zip > artifact/SHA256.txt

      - uses: actions/upload-artifact@v4
        with:
          name: cardcha-alpha28-056-card-auto-scenario-runner
          path: artifact
          if-no-files-found: error
