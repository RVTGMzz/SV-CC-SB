name: Cardcha alpha27.0.7.7.4 attic + broom final (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo '0.7.7.4 is archived; use the latest alpha.27 workflow.'
