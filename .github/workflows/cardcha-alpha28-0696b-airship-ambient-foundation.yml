name: Cardcha 0696B Airship Ambient Runtime Foundation (legacy)

# 0696C supersedes this foundation. Keep only as a manual historical replay;
# it must never rematerialize .63 over the active Airship recovery branch.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout historical branch state
        uses: actions/checkout@v4
      - name: Explain retirement
        run: |
          echo '0696B is frozen. Continue with the 0696C Airship Deck Visual Recovery workflow.'
