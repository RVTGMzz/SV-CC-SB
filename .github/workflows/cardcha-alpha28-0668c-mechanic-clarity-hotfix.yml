name: Cardcha 0668C First-Pass Workflow (Archived)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived after validator-only first-pass failure; active workflow is the v2 file."
