name: Cardcha 0696D1 Observation Window Source Cleanup (Rejected Legacy)

# .65 was visually rejected by Ron: the fuzzy matte cleanup clipped real sprite
# edges and the generated hollow frame contradicted the approved 0690 four-overlay
# contract. Keep this workflow manual-only for historical evidence. D1R supersedes it.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-evidence-only:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "0696D1 .65 is VISUAL REJECTED."
          echo "Use cardcha-alpha28-0696d1r-window-bounded-cleanup workflow instead."
