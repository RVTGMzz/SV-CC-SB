name: Cardcha 0696D1R Window Bounded Cleanup (maintenance pause)

# Temporarily manual-only while the final detached-edge-bleed gate is being added.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "D1R maintenance pause. Push workflow will be restored after the bleed gate lands."
