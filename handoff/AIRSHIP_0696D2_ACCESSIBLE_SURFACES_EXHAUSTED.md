# AIRSHIP 0696D2 Accessible Recovery Surfaces Exhausted

Date: 2026-09-14
Branch: `cardcha-alpha28-0696d2-window-environment-matrix`
Status: **BLOCKED ON AUTHORITATIVE EXTERNAL/LOCAL SOURCE BYTES**

## Exact gate remains authoritative

The four D2.1 Observation Window environment sources must be exact PNG / 160x80 / RGBA byte streams matching:

- morning: `9a8996ef0068057e774419d9d4597f44753951b3ec1ec519cd6771efaad7b5d0`
- noon: `fa0f00051febb62d107e4a1dc14a079dbfc8183659a73b9ed2ecfe5c4efb3aff`
- evening: `b94d30d18bacecc2c3f09971aec2d5951956490be8c7cad2243b4689c865a638`
- night: `f9d15f66485f514f4be8589ee29d45beabda87e357b1c1d3ffa7965855ed1990`

No approximation, redraw, resize, recolor, re-encode, upscale, legacy-overlay reuse, or visual lookalike is permitted.

## Accessible recovery surfaces completed

The currently available chat/tooling environment has now exhausted the following routes without finding any of the four exact byte streams:

1. current Git lineage and exact target-path history;
2. all relevant live 0696 branches;
3. historical D1/D1R/D1S and `.63 -> .68` package artifacts;
4. all documented 0696 concept/integration GitHub Actions runs;
5. File Library exact-name/hash/archive/date/visual searches;
6. recoverable conversation metadata and personal-context provenance tracing;
7. connected Google Drive exact archive/project searches;
8. connected Google Drive generic export/image-name searches;
9. connected Google Drive active PNG date/MIME searches for 2026-09-12 and 2026-09-13;
10. connected Google Drive Trash exact-name and date/MIME searches;
11. connected Gmail attachment searches for archive aliases, Cardcha/Airship, exact production filenames, PNGs, and any attachment during the D2.1 window;
12. connected Canva existing-design/folder searches for Cardcha/Airship/Shardbound/0696/Observation Window;
13. shared ChatGPT public URL fetch/index attempt;
14. current sandbox residue inspection;
15. D2 branch commit-window audit around the recovered `hash-checked materializer` conversation clue;
16. GitHub Issues/PR collaboration surface searches for 0696/Airship/Observation Window/image-attachment references;
17. GitHub Releases collection;
18. indexed public-web searches for all four authoritative SHA256 values, all four production filenames, source-archive aliases, and project terms.

## Important provenance conclusion

Conversation evidence shows that a `hash-checked materializer` was referenced immediately before the D2 checkpoint and that the four approved scenes had been normalized to production 160x80 with hashes recorded.

However, no committed helper, source path, attachment ID, image-generation ID, sandbox path, archive member name, or source byte stream survives in any currently accessible surface.

The strongest evidence therefore points to a transient original-chat/local-sandbox/image-export step whose byte inputs were never persisted into repository-accessible storage.

## What can still unlock D2.1

At least one genuinely new authoritative source must enter the workflow, for example:

- original D2.1 image-generation/export files from the old chat/session or local Downloads folder;
- a local-machine copy of `concept(1).rar` / `sprite(1).rar`;
- another local archive/folder/export containing the exact approved files;
- a newly connected external storage location that actually contains one of those original files.

When such a source is available, run:

```bash
python3 tools/alpha28_0696d2_recover_exact_sources.py /path/to/candidates
```

Only after all four exact SHA256 values are found:

```bash
python3 tools/alpha28_0696d2_recover_exact_sources.py /path/to/candidates --install
python3 tools/alpha28_0696d2_window_environment_matrix.py --check-source
python3 tools/alpha28_0696d2_window_environment_matrix.py --apply
python3 tools/alpha28_0696d2_window_environment_matrix.py --validate
```

Then continue:

`compile -> package audit -> .69 TEST -> Ron in-game visual acceptance`

## Hard stop

Until genuinely new authoritative source bytes are supplied or connected, further searches of the already audited surfaces are duplicate work and must not be used to simulate progress.

`.69` remains intentionally unmaterialized. D1S airship remains frozen. Navigation Console remains deferred.
