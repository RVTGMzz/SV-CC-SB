+5. Portable milestone text is synchronized to **20 unique cards** in EN/VI. Current direct 20-card gift remains only as a compatibility bridge until the approved milestone quest/boss flow is implemented.
+6. Binder left page no longer shows the text labels `Túi đồ` or `Bộ bài đang dùng`.
+7. `Trang x/y` stays in its existing footer position; `Đã khám phá x/80 lá` sits compactly to its left on the same line.
+8. Five normal slots + Boss + ChaCha/Secret are equal-size enlarged circles.
+9. Boss circle has Mythic rarity border and no `BOSS` text.
+10. ChaCha/Secret circle has a pink border and no `SECRET` text.
+11. Left rarity bookmarks are slightly wider so long Vietnamese labels such as `Thần Thoại` do not clip.
 
-A complete source snapshot of the current v0.3 Binder work is preserved at:
+## Approved progression direction after alpha.11 UI pass
 
-`snapshots/Cardcha_v0.3.0-alpha.1_BinderUI_SOURCE_SNAPSHOT.zip`
+This is approved design but **not fully implemented in alpha.11**:
 
-The snapshot has been advanced from the old 10-card/11.45 Binder source toward `0.3.0-alpha.1`:
+- **20 unique cards** → ChaCha/Secret information begins to unlock → milestone quest → Boss encounter → Portable Cardcha Machine reward.
+- **40 unique cards** → next quest/encounter → ChaCha fuses/absorbs the stationary + portable machines → Cardcha becomes directly accessible from the Binder.
+- ChaCha/Secret circle is the story/system-progression slot; Boss Slot remains the combat Boss Card / Boss Energy slot.
+- After 20, ChaCha transformation information/mechanics can begin to unfold; later Boss Card activation remains the combat transformation route.
 
-- synchronized the approved **80-card visual/data snapshot** from `Cardcha_v0.2.0-beta.2_80CardVisuals_TEST`;
-- Base IDs are 1–80 and IconIndex is 0–79;
-- synchronized the 80-card atlas and bilingual i18n snapshot;
-- added Binder v0.3 favorite persistence (`SaveData.SchemaVersion = 12`);
-- rebuilt Binder around a near-full-screen two-page collection-book layout;
-- collection is icon-only with Base ID + Lv + rarity border;
-- locked cards retain their real rarity border and render as dark silhouette/grayscale;
-- equipped cards receive an in-cell `IN USE / ĐANG DÙNG` overlay;
-- rarity bookmarks filter the collection while preserving Base-ID order;
-- Favorites is a separate bottom bookmark and persists by string card ID;
-- five active slots are circular and the future Boss Slot is shown as a separate locked circle;
-- right page contains readable selected-card details and actions;
-- controller A is explicitly consumed as an in-book action and cannot close the Binder;
-- the center seam is intentionally thin; no `assets/binder_book_frame.png` is required by the new Binder class.
+## Mandatory test order
 
-## Mandatory first test on Windows/Stardew
+1. Build on Windows with Stardew + SMAPI references.
+2. Open Binder and test controller traversal into all three detail action buttons.
+3. Test double mouse click and double controller A on owned/equipped cards.
+4. Verify footer/layout at the user's normal UI scale.
+5. Verify Boss/ChaCha circles are equal size and use Mythic/pink borders.
+6. Test one stationary and one portable pull: flash should read across the full card/panel.
+7. Check MiMi portable milestone text in both EN and VI.
+8. Regression-test story handoff, stationary machine, portable machine, gacha, Scrap, combat HUD, and ChaCha follow.
 
-This source snapshot was prepared without a local Stardew/SMAPI build environment, so the first next action is a real build/test with `BUILD_CARDCHA.bat` before treating it as a playable release.
+## Important caveat
 
-Check in this order:
-
-1. project compiles after the Binder class replacement;
-2. open Binder from Machine and inventory Book tab;
-3. verify 80 cards are visible across four All pages;
-4. verify locked card = real rarity border + silhouette;
-5. verify Favorite add/remove survives save/reload;
-6. verify rarity/favorite bookmarks filter correctly and preserve Base-ID order;
-7. verify equipped overlay appears on the matching Collection cell;
-8. verify A never exits the book; B/back still returns correctly;
-9. verify text is readable at the user's normal UI scale;
-10. verify no missing `binder_book_frame.png` warning occurs from Binder;
-11. regression-test MiMi, portable machine, gacha and combat HUD.
-
-## Critical caveat: 80-card data vs runtime effects
-
-The 80-card visual snapshot contains many effect keys that are newer than the old 10-card source baseline. Treat the preserved snapshot as authoritative for the **collection data/art/UI work**, but audit actual Combat/Drop/Gacha implementation before claiming all 80 card effects are active.
-
-Do not silently reduce the registry back to the old 10-card `cards.json`.
+The 80-card data/art registry is present, but not every one of the 80 runtime effect keys is necessarily implemented in Combat/Drop/Gacha yet. Audit those separately before claiming full 80-card gameplay coverage.
diff --git a/README_ALPHA11_TEST.txt b/README_ALPHA11_TEST.txt
new file mode 100644
index 0000000..0331603
--- /dev/null
+++ b/README_ALPHA11_TEST.txt
@@ -0,0 +1,20 @@
+Cardcha v0.3.0-alpha.11 — BINDER INPUT + FULL FLASH — FULL WINDOWS BUILDER
+
+CACH DUNG:
+1. Giai nen TOAN BO ZIP nay vao mot thu muc rieng.
+2. Tat Stardew Valley + SMAPI.
+3. Chay BUILD_ALPHA11_TEST.bat.
+4. Neu BUILD PASSED: lay file _READY_TO_TEST\Cardcha_v0.3.0-alpha.11_BinderInputFullFlash_TEST.zip.
+5. Cai ZIP test vao Mods/Cardcha nhu binh thuong.
+6. Neu BUILD FAILED: gui build-alpha11-log.txt.
+
+Builder nay da co san full source alpha.11, khong can clone GitHub hay copy patch vao repo khac.
+No khong tu dong cai mod vao Stardew Valley.
+
+TEST UU TIEN:
+- 3 nut Favorite / Trang bi / Nang cap bang controller A.
+- Double click / double A de trang bi-thao nhanh.
+- Flash quay full o/full khung.
+- Footer: Da kham pha x/80 nam ben trai, Trang x/y giu nguyen vi tri.
+- 7 o tron bang nhau; Boss vien Mythic; ChaCha/Secret vien hong, khong con chu BOSS/SECRET.
+- Text portable EN/VI deu dung moc 20.
diff --git a/design/BINDER_V0_3_CURRENT.md b/design/BINDER_V0_3_CURRENT.md
index 4528be6..65ac1c0 100644
--- a/design/BINDER_V0_3_CURRENT.md
+++ b/design/BINDER_V0_3_CURRENT.md
@@ -159,3 +159,23 @@ Not part of this UI pass:
 ## Important implementation caveat
 
 The synchronized `0.2.0-beta.2_80CardVisuals_TEST` data contains 80 cards and many effect keys beyond the old 10-card prototype source. The visual/data sync is intentional so the repository no longer loses the approved Base Set snapshot, but **do not assume every one of those 80 runtime effects is implemented merely because its card data exists**. Runtime effect implementation must be audited separately against Combat/Drop/Gacha services.
+
+## Alpha.11 approved UI corrections — 2026-08-26
+
+- Detail action controls must be fully controller/keyboard operable.
+- Double mouse click / double controller confirm on the same collection card toggles equip/unequip.
+- Remove visible `Túi đồ` text; keep a compact back icon/control only.
+- Remove `Bộ bài đang dùng` heading.
+- Keep `Trang x/y` exactly in the current footer position and place compact `Đã khám phá x/80 lá` to its left.
+- Enlarge the circular loadout row; five normal slots + Boss + ChaCha/Secret are equal size for now.
+- Boss slot: no BOSS label; use Mythic rarity border.
+- ChaCha/Secret slot: no SECRET label; use pink border.
+- Portable/reveal animation should pulse the full card/panel, not a small local flicker.
+- Portable milestone text is synchronized to 20 unique cards.
+
+### Progression direction locked with this pass
+
+- 20 unique cards: ChaCha info begins to unlock, then quest + Boss encounter rewards Portable Machine.
+- 40 unique cards: follow-up quest/encounter lets ChaCha absorb/fuse both machines and move direct Cardcha access into the Binder.
+- ChaCha/Secret slot is story/system progression; Boss Slot remains Boss Card combat progression.
+- The old direct 20-card gift may remain temporarily in test builds only until the quest/boss implementation replaces it.
diff --git a/src/Cardcha/Cardcha.csproj b/src/Cardcha/Cardcha.csproj
index 9ab3286..afea967 100644
--- a/src/Cardcha/Cardcha.csproj
+++ b/src/Cardcha/Cardcha.csproj
@@ -7,7 +7,7 @@
     <EnableHarmony>true</EnableHarmony>
     <AssemblyName>Cardcha</AssemblyName>
     <RootNamespace>Cardcha</RootNamespace>
-    <Version>0.3.0-alpha.10</Version>
+    <Version>0.3.0-alpha.11</Version>
   </PropertyGroup>
 
   <ItemGroup>
diff --git a/src/Cardcha/ModEntry.cs b/src/Cardcha/ModEntry.cs
index aba89c4..33d3baa 100644
--- a/src/Cardcha/ModEntry.cs
+++ b/src/Cardcha/ModEntry.cs
@@ -161,7 +161,7 @@ internal sealed class ModEntry : Mod
         BookNavigationPatch.Apply(harmony, this.BookTab);
 
         this.Monitor.Log(
-            $"Cardcha! v0.3.0-alpha.10 LIGHT BINDER TAB ICON with {this.Cards.All.Count} cards. The cardboard is now combat-capable. This seems unsafe.",
+            $"Cardcha! v0.3.0-alpha.11 BINDER INPUT + FULL FLASH with {this.Cards.All.Count} cards. The cardboard is now combat-capable. This seems unsafe.",
             LogLevel.Info
         );
     }
@@ -605,7 +605,7 @@ internal sealed class ModEntry : Mod
     private void CommandLootRates(string command, string[] args)
     {
         this.Monitor.Log(
-            "===== CARDCHA LOOT RATES v0.3.0-alpha.10 =====\n" +
+            "===== CARDCHA LOOT RATES v0.3.0-alpha.11 =====\n" +
             "Regular enemy: 12% normal Scrap, amount 1; dry-streak guarantee at 8 kills; Shiny 3%, amount 1.\n" +
             "Boss-like (boss/elite/apex/champion/raid hint): 65% normal Scrap, amount 2; Shiny 25%, amount 1.\n" +
             "Raw Max HP is NOT used to classify or scale rewards.",
@@ -696,7 +696,7 @@ internal sealed class ModEntry : Mod
     private void CommandVersion(string command, string[] args)
     {
         this.Monitor.Log(
-            "Cardcha! v0.3.0-alpha.10 LIGHT BINDER TAB ICON",
+            "Cardcha! v0.3.0-alpha.11 BINDER INPUT + FULL FLASH",
             LogLevel.Alert
         );
     }
diff --git a/src/Cardcha/Services/PortableMachineService.cs b/src/Cardcha/Services/PortableMachineService.cs
index 243af71..70b913a 100644
--- a/src/Cardcha/Services/PortableMachineService.cs
+++ b/src/Cardcha/Services/PortableMachineService.cs
@@ -62,7 +62,8 @@ internal sealed class PortableMachineService
         }
     }
 
-    /// <summary>Claim the 50-unique-card gift when the player next talks business with MiMi.</summary>
+    /// <summary>Claim the current 20-unique-card milestone gift when the player next talks business with MiMi.
+    /// This direct gift is a compatibility bridge until the approved 20-card quest + boss reward is implemented.</summary>
     public bool TryGrantMilestoneGift()
     {
         if (!this.IsMilestoneGiftReady)
diff --git a/src/Cardcha/UI/CardchaBinderMenu.cs b/src/Cardcha/UI/CardchaBinderMenu.cs
index 51a4743..f945919 100644
--- a/src/Cardcha/UI/CardchaBinderMenu.cs
+++ b/src/Cardcha/UI/CardchaBinderMenu.cs
@@ -75,6 +75,9 @@ internal sealed class CardchaBinderMenu : IClickableMenu
     private int CurrentPage;
     private CardDefinition? Selected;
     private CardDefinition? ControllerCardContext;
+    private string? LastQuickToggleCardId;
+    private double LastQuickToggleAtMs;
+    private const double QuickToggleWindowMs = 600d;
     private string Status = ModEntry.T("binder.status.pick");
 
     private enum BinderFilter
@@ -122,15 +125,25 @@ internal sealed class CardchaBinderMenu : IClickableMenu
         this.LeftPage = new Rectangle(paper.X, paper.Y, pageWidth, paper.Height);
         this.RightPage = new Rectangle(this.LeftPage.Right + seam, paper.Y, paper.Width - pageWidth - seam, paper.Height);
 
+        // Keep a compact icon-only back control. The previous text label (e.g. "Túi đồ")
+        // consumed valuable header space and duplicated context the player can already see.
         this.BackButton = new ClickableComponent(
-            new Rectangle(this.LeftPage.X + 18, this.LeftPage.Y + 14, Math.Min(170, this.LeftPage.Width / 3), 42),
+            new Rectangle(this.LeftPage.X + 18, this.LeftPage.Y + 14, 52, 42),
             "back") { myID = BackId };
 
-        // Keep the progress line and loadout heading on separate baselines.
-        int activeY = this.LeftPage.Y + 126;
-        int activeDiameter = Math.Clamp((this.LeftPage.Width - 176) / 7, 42, 58);
-        int activeGap = 5;
-        int activeX = this.LeftPage.X + 18;
+        // v0.3 alpha.11: use the reclaimed header space for a larger, even 7-circle row.
+        // Five normal slots + Boss + ChaCha/Secret are intentionally the same diameter.
+        int activeGap = 8;
+        int totalCircleCount = VisualActiveSlots + 2;
+        int activeAvailableWidth = this.LeftPage.Width - 44;
