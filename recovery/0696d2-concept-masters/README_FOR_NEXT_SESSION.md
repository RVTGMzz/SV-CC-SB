# Cardcha 0696D2 Observation Window — Recovered Concept Library

This archive is a **source/concept checkpoint only** for future Cardcha sessions.

## Important rules
- These are the original recovered image bytes from the current chat/session.
- Files were **renamed only** inside this archive for clarity.
- No image was resized, cropped, converted, re-encoded, color-converted, or otherwise modified.
- Do **not** treat these as production 160x80 sprites.
- Do **not** regenerate approved masters unless Ron explicitly requests it.
- The four files in `01_approved_time_of_day_masters/` are the primary approved D2.1 Morning / Noon / Evening / Night masters.
- The set in `02_weather_set/` now contains four named concepts: Clear / Rain / Thunderstorm / Snow.
- Files in `05_earlier_concepts_with_airship/` contain an airship baked into the scene and should not silently replace the later architecture where the flying airship is a separate runtime sprite.

## About the old `imagegen.png`
The generic `imagegen.png` filename has been removed from the clean naming scheme.
It is preserved as:
`04_additional_variants/observation_window_variant_overcast_rain_duplicate_export.png`

Its bytes are identical to:
`04_additional_variants/observation_window_variant_overcast_rain.png`

See `MANIFEST_SHA256.csv` and `SOURCE_NAME_MAP.md` for full provenance and hashes.
